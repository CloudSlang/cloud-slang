#   (c) Copyright 2014 Hewlett-Packard Development Company, L.P.
#   All rights reserved. This program and the accompanying materials
#   are made available under the terms of the Apache License v2.0 which accompany this distribution.
#
#   The Apache License is available at
#   http://www.apache.org/licenses/LICENSE-2.0

namespace: docker.maintenance.ops

operations:
  - check_mysql_is_up:
        inputs:
          - containerID
          - cmdParams: "'-i'"
          - host
          - port: "'22'"
          - username
          - password
          - privateKeyFile: "''"
          - arguments: "''"
          - mysqlUsername
          - mysqlPassword
          - execCmd: "'mysqladmin -u ' + mysqlUsername + ' -p' + mysqlPassword + ' ping'" # | awk \\'{print $3}\\''"
          - command: "'docker exec ' + cmdParams + ' ' + containerID + ' ' + execCmd"
          - characterSet: "'UTF-8'"
          - pty: "'false'"
          - timeout: "'90000'"
          - closeSession: "'false'"
        action:
          java_action:
            className: org.openscore.content.ssh.actions.SSHShellCommandAction
            methodName: runSshShellCommand
        outputs:
          - command: fromInputs['command'] # TODO: remove this outputs
          - returnResult
          - STDOUT
          - STDERR
          - returnCode
          - result: returnResult
          - errorMessage: STDERR if returnCode == '0' else returnResult
        results:
          - SUCCESS : returnCode == '0' and (not 'Error' in STDERR) # TODO: refactor results test - check 'alive'
          - FAILURE
